import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css"; // opzionale
import ManuscriptAnalyzer from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <ManuscriptAnalyzer />
  </React.StrictMode>
);
